import Section from '../../../components/case-study/Section';

export default function Context() {
  return (
    <Section id="context" eyebrow="CONTEXT">
      <p className="font-dm text-[16px] text-black leading-[23px]">
        In EdTech, a product reaches three layers of people: the decision-maker who buys it, the IT admin who installs it, and the teacher who actually uses it. A campaign can reach one layer and completely miss another. When myViewBoard 3.0 launched, that gap is exactly where it fell.
      </p>
    </Section>
  );
}
